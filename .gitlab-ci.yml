stages:
  - build
  - lint
  - test

before_script:
  - echo $CI_JOB_TOKEN | docker login -u gitlab-ci-token --password-stdin $CI_REGISTRY

build:
  stage: build
  script:
    - make build
    - make push
lint:
  stage: lint
  script:
    - make lint

test:
  stage: test
  script:
    - make test
